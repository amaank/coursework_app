.jumbotron.text-center
  %h1= t('.title')
  %p= t('.description')

.fill-space

.container.fill-space
  .page-header
    %h3= t('.form_heading')
    %p= t('.form_description')

  // Create form for contact details, to be submitted to request_contact action of home
  = form_tag request_contact_path, :id => 'contact_form', method: 'post' do
    .form-group
      = label_tag :name, t('.name')
      = text_field_tag :name, '', title: t('.enter_name'), size: 50, class: "form-control"

    .form-group
      = label_tag :email, t('.email')
      = email_field_tag :email, '', title: t('.enter_email'), class: "form-control"

    .form-group
      = label_tag :telephone, t('.telephone')
      = telephone_field_tag :telephone, '', title: t('.enter_telephone'), class: "form-control"

    .form-group
      = label_tag :message, t('.message')
      = text_field_tag :message, '', title: t('.enter_message'), size: 100, class: "form-control"

    = submit_tag t('.submit'), class: "btn btn-default"

  %br

.fill-space
